# Query Patterns: Messaging

**FSD Requirement**: FR-1.4

---

## Overview

Query patterns for the Messaging entity group (`message_history`, `stream_metadata`).

No queries exist yet. Documented patterns will be populated as SQLC queries are added.
