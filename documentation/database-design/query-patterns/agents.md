# Query Patterns: Agents

**FSD Requirement**: FR-1.4

---

## Overview

Query patterns for the Agents entity group (`agents`, `agent_configs`, `agent_prompts`, `sessions`).

No queries exist yet. Documented patterns will be populated as SQLC queries are added.
