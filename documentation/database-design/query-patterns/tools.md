# Query Patterns: Tools

**FSD Requirement**: FR-1.4

---

## Overview

Query patterns for the Tools entity group (`tools`, `tool_invocations`, `skills`, `agent_skills`).

No queries exist yet. Documented patterns will be populated as SQLC queries are added.
