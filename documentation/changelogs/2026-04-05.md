# 2026-04-05

## Changes

### Added
- Phase 7: Tag index management using Valkey sets (SADD/SMEMBERS/SREM) for tag-based invalidation
- Phase 7: Reverse lookup (`_keytags:{key}`) for tag cleanup on key deletion
- Phase 7: `InvalidateByVersion` method — versioned cache invalidation with tag index cleanup
- Phase 7: `SAdd`, `SMembers`, `SRem` methods to `CacheBackend` interface and `valkeyBackend` implementation
- Phase 7: 7 new test cases covering tag index lifecycle and versioned invalidation

### Changed
- `Set` and `SetMany` now use `SADD` for tag index (previously used `SET`, incompatible with `DeleteByTag`)
- `Delete` and `DeleteMany` now clean up tag index (forward and reverse lookup) before removing keys
- `SAdd` uses `DoMulti` for atomic SADD + EXPIRE pipelining

## Notes
Phase 7 QA found 4 issues (1 MEDIUM, 3 LOW) — all fixed before PR. Tag index was broken in Phase 6 (used SET instead of SADD), now corrected.
