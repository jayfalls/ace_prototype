# 2026-03-12

## Changes

### PR #71 - Fix: Use Dockerfile.dev in docker-compose.dev.yml

#### Fixed
- Fixed docker-compose.dev.yml to use `devops/Containerfile.api.dev` instead of incorrect path

### PR #70 - Phase 6: Integration - Verify all services work together

#### Added
- Split Dockerfiles into dev and prod variants:
  - `api/Dockerfile.dev` - Hot reload with air, runs as root for volume permissions
  - `api/Dockerfile.prod` - Pre-built binary, runs as non-root appuser
  - `frontend/Dockerfile.dev` - Development server with npm run dev
  - `frontend/Dockerfile.prod` - Production build, runs as non-root appuser
- Split Docker Compose into dev and prod:
  - `docker-compose.dev.yml` - Development with volume mounts for hot reload
  - `docker-compose.prod.yml` - Production without volume mounts

#### Changed
- Updated Makefile to support ENVIRONMENT variable (dev/prod, default: dev)
- Validates both CONTAINER_ORCHESTRATOR and ENVIRONMENT values

#### Fixed
- Fixed PostgreSQL 18+ volume path (`/var/lib/postgresql` instead of `/var/lib/postgresql/data`)
- Fixed air.toml tmp_dir to use `/tmp/ace-api` for write permissions
- Fixed container startup issues by adjusting user permissions for dev mode

#### Acceptance Criteria Met
- ✓ All four services defined in docker-compose (ace_api, ace_fe, ace_db, ace_broker)
- ✓ Services on same network and can communicate
- ✓ Makefile respects CONTAINER_ORCHESTRATOR env var
- ✓ Makefile respects ENVIRONMENT env var
- ✓ `make up` starts all services in dev mode
- ✓ `make down` stops all services
- ✓ `make up ENVIRONMENT=prod` starts all services in prod mode

### PR #69 - Phase 3: Frontend Service - SvelteKit with hot reload

#### Added
- Created `frontend/` directory with:
  - `Dockerfile` - Multi-stage build for SvelteKit container
  - `package.json` - Dependencies: SvelteKit 2.x, Vite 6.x, TypeScript
  - `svelte.config.js` - SvelteKit configuration with Node adapter
  - `vite.config.ts` - Vite config with HMR for hot reload (0.0.0.0:5173)
  - `src/app.html` - Minimal SvelteKit HTML template
  - `src/routes/+page.svelte` - Welcome page component
  - `src/routes/+layout.svelte` - Layout component
  - `src/app.d.ts` - TypeScript declarations
  - `tsconfig.json` - TypeScript configuration
  - `.dockerignore` - Docker ignore patterns
- Updated `docker-compose.yml` with ace_fe service:
  - Added `npm run dev` command for hot reload
  - Added `NODE_ENV=development` environment variable
  - Volume mount preserves node_modules while syncing source

#### Acceptance Criteria Met
- ✓ Dockerfile builds successfully
- ✓ package.json has valid dependencies
- ✓ docker-compose.yml includes ace_fe service
- ✓ Frontend accessible on port 5173

### PR #68 - Phase 2: API Service - Go backend with hot reload

#### Added
- Created `api/` directory with:
  - `Dockerfile` - Multi-stage build for dev and prod (with non-root user)
  - `go.mod` - Go module with Gin web framework dependency
  - `main.go` - Minimal HTTP server using Gin on port 8080
  - `air.toml` - Hot reload configuration for development
  - `go.sum` - Go dependencies lock file
- API endpoints:
  - `GET /` - Returns `{"message":"ACE API Server"}`
  - `GET /health` - Returns `{"status":"OK"}`

#### Changed
- Switched from standard library `net/http` to Gin web framework
- Updated Dockerfile to run as non-root user (`appuser`)

#### Documentation Added
- `documentation/getting-started.md` - Complete guide for setting up and using the development environment

### PR #67 - Phase 1: Foundation

#### Added
- Created root directory structure
- Created `docker-compose.yml` with all four services:
  - `ace_api` - Go API service (port 8080)
  - `ace_fe` - SvelteKit frontend (port 5173)
  - `ace_db` - PostgreSQL database (port 5432)
  - `ace_broker` - NATS messaging (port 4222)
- Created `.env.example` template with all required environment variables
- Created `.gitignore` for development environment
- Created `Makefile` with CONTAINER_ORCHESTRATOR support (docker/podman)

#### Verified
- docker-compose.yml validates with `docker compose config`

### PR #66 - Create GitHub Issues for Core-Infra Implementation

#### Added
- Created 4 GitHub issues for core-infra implementation phases:
  - #62 - Phase 1: Foundation
  - #63 - Phase 2: API Service
  - #64 - Phase 3: Frontend Service
  - #65 - Phase 6: Integration

### Template PR #53 - Add Research Template

#### Added
- New `research.md` template document for researching industry standards and evaluating different approaches before making design decisions
- Updated template README.md to include research document and updated workflow
- Updated AGENTS.md to include research template (now 14 documents instead of 13)
- Updated workflow to place research after user_stories (BSD → user_stories → research → FSD)

### PR #56 - Core Infrastructure BSD

#### Added
- Created Business Specification Document for core infrastructure
- Goal: Lay groundwork for entire project (Go backend, Svelte frontend, database, NATS)
- Development driven exclusively via docker/podman
- Nothing needs to function yet, just get everything running

### PR #57 - Core Infrastructure User Stories

#### Added
- Created user_stories.md with acceptance criteria for core infrastructure

### PR #58 - Core Infrastructure Research

#### Added
- Created research.md evaluating approaches for development environment

### PR #59 - Core Infrastructure FSD

#### Added
- Problem space discovery via question loop with user
- Created `problem_space.md` documenting:
  - Core requirements: single command setup, hot reload, always-run-together
  - AI-first principle: agent-friendly code and tooling
  - Dev=Prod: same base images, compose for single agent, K8s for multi
  - Max automation: auto migrations and seeding
  - Resource expectations: 4vCPU/8GB reported, 2vCPU/2GB actual minimum
- Updated FSD with latest stable versions:
  - Go 1.26 (was 1.21)
  - Node.js 25 (was 20)
  - PostgreSQL 18 (was 16)
  - NATS 2.12 (was 2.10)
- Added `CONTAINER_ORCHESTRATOR` env var for docker/podman selection
- Service naming with `ace_` prefix: ace_api, ace_fe, ace_db, ace_broker
- Changed to single Dockerfile (not Dockerfile.dev)
- Renamed `backend/` to `api/`
- Updated Makefile commands for container orchestrator
- Updated template with problem_space.md (15 documents total)

#### Updated AGENTS.md
- Added Problem Space Discovery section (question loop before BSD)
- Added Technology Recommendations section (web search requirement)
- Added Post-Merge Workflow (update changelog after PR)
- Added Unit Completion Workflow (create micro-PR issues when docs approved)
- Updated research template with web search requirement

## Notes
- Research template comes after user_stories in the workflow to ensure user requirements are understood before researching solutions
- Template includes sections for: industry standards, alternative approaches, comparison matrix, recommended approach, rationale, risks/mitigations, and references

### PR #60 - Core Infrastructure Architecture

#### Added
- Created architecture.md with technical architecture decisions
- Single docker-compose.yml for all environments (dev/prod via env)
- Service discovery via Compose DNS (K8s-ready)
- Environment-driven configuration
- Scaffolding for observability and migrations
- Kubernetes-ready structure

#### Updated AGENTS.md
- Updated Problem Space Discovery to cover each document (not just BSD)
- Agent must explore each document with questions before writing

### PR #61 - Core Infrastructure Implementation

#### Added
- Created implementation.md with highly verbose, detailed task breakdown
- Six phases: Foundation, API, Frontend, Database, Messaging, Integration
- Each task includes description, dependencies, and time estimate
- Each task has verification step
- Final integration verification section
- Rollback plan included

#### Updated Template
- Updated template/implementation.md with guidelines
- Be highly verbose, break down into smallest tasks
- Document WHAT needs to be created, not HOW
- Include verification for EACH task
- Include final integration verification
- Order tasks logically (dependencies first)

### GitHub Issues Created

Created 4 GitHub issues for core-infra implementation:
- [#62](https://github.com/jayfalls/ace_prototype/issues/62) - Phase 1: Foundation - docker-compose, Makefile, env
- [#63](https://github.com/jayfalls/ace_prototype/issues/63) - Phase 2: API Service - Go backend with hot reload
- [#64](https://github.com/jayfalls/ace_prototype/issues/64) - Phase 3: Frontend Service - SvelteKit with hot reload
- [#65](https://github.com/jayfalls/ace_prototype/issues/65) - Phase 6: Integration - Verify all services work together

Each issue includes:
- Context and tasks
- Acceptance criteria
- Dependencies on prior issues
- Instructions to respond with PR link once created
