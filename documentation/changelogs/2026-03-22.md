# 2026-03-22

## Changes

### Added
- Created `documentation/database-guide.md` — unified guide for reading, writing, and maintaining database docs
- Added documentation validation step to pre-commit hook (step 5/9)
- Added `GOWORK=off` for doc scripts (standalone modules inside backend/ workspace)
- Created `design/units/cleanup/problem_space.md` — Problem space for comprehensive QA review unit
- Created `design/units/cleanup/review-plan.md` — Systematic review plan for QA review unit

### Changed
- Moved `scripts/` to `backend/scripts/` (consistent with Go workspace structure)
- Merged `make docs` into `make test` (single entry point)
- Doc validation in `make test` runs through `$(ORCHESTRATOR) exec ace_api` (DB guaranteed)
- Doc validation in pre-commit skips gracefully when no DB (flags for agent)
- Updated `design/README.md` — removed separate `make docs`, updated `make test` description
- Updated pre-commit hook from 8 to 9 gates (fixed gate numbering 1/9 through 9/9)
- Updated all `make docs` references to `make test` in unit documentation
- Updated `AGENTS.md` — added critical workflow rules: wait for merge, mandatory changelogs

### Removed
- `scripts/` directory (moved to `backend/scripts/`)
- `make docs` separate Makefile target (merged into `make test`)
- Duplicate `re:` target in Makefile
- `docs` from Makefile .PHONY list

### Fixed
- Pre-commit hook gate numbering (was mixed 1/8-4/8 and 5/9-9/9, now all 1/9-9/9)
- Pre-commit hook uses correct path (`scripts/docs-gen`) and `GOWORK=off`
- Makefile doc validation runs inside container via orchestrator exec

## Agent Updates
- `.opencode/agents/backend.md` — added 8 database doc references
- `.opencode/agents/planning.md` — added database-guide.md reference
- `.opencode/agents/technical.md` — added database docs and patterns references
- `.opencode/agents/qa.md` — added database docs reference for code review

## Notes
- Doc scripts require `GOWORK=off` when run locally (standalone modules in backend/ workspace)
- `make test` runs doc validation inside API container (DB guaranteed)
- Pre-commit skips doc validation when no DB, but flags it with warning
- PR #186 (integration cleanup), PR #187 (guide rename) already merged
- PR #188 (fixes: gate numbering, make test via container, unified scripts, Annot8, all doc gaps filled)
- PR #189 (design README update)
- PR #191 (cleanup unit problem_space.md + AGENTS.md workflow rules)
- PR #192 (cleanup unit review-plan.md)
- PR #193 (long-term memory update with unit-docs-snapshots learning)
