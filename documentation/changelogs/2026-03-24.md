# 2026-03-24

## Changes

### Added
- Created `design/units/caching-strategies/user_stories.md` — 47 Gherkin scenarios across 9 features covering core library usage, frontend caching, backend tool selection, invalidation strategies, observability, stampede protection, cache warming, multi-agent isolation, and testing strategy
- Created `design/units/caching-strategies/fsd.md` — Functional Specification Document with Go interfaces, cache key conventions, invalidation algorithms, and performance requirements
- Created `design/units/caching-strategies/research.md` — Technology evaluation covering 17 approaches across 4 categories: distributed cache backends (Valkey, Redis, Memcached, Dragonfly), in-memory libraries (Otter, Ristretto, BigCache, FreeCache, go-cache, sturdyc, + 7 more), invalidation patterns (NATS-based, version stamps, event sourcing), and stampede protection (singleflight, resenje/singleflight)
- Created `design/units/caching-strategies/dependencies.md` — Dependencies and version constraints with Valkey as hard requirement (valkey-go ^1.0.73, x/sync ^0.15.0)
- Created `design/units/caching-strategies/architecture.md` — Technical architecture for shared/caching: component diagrams, data flows, invalidation strategies, stampede protection, cache warming, observability integration, multi-agent isolation

- Created `design/units/caching-strategies/api.md` — Go API specification documenting Cache, CacheBackend, CacheObserver, SingleFlight interfaces, all public types, constructor/option functions, 14 sentinel error types, and usage examples for cache-aside, bulk operations, pattern/tag/versioned invalidation
- Created `design/units/caching-strategies/security.md` — Security considerations: data protection, input validation, STRIDE threat model (18 threats), key isolation, connection security (TLS), Valkey ACLs, security testing checklist, incident response procedures

### Changed
- Fixed naming collision in Cache interface — `WithTTL`/`WithTags` renamed to `WithDefaultTTL`/`WithDefaultTags` (Cache-scoped defaults) to avoid conflict with SetOption functions (per-operation overrides). Updated across api.md, fsd.md, architecture.md
- Updated `KeyBuilder.Build()` and `Pattern()` signatures to return `(string, error)` in fsd.md for proper error handling
- Simplified `.opencode/agents/orchestrator.md` — Reduced from 621 to 236 lines, merged 18 sections into 7 clear sections
- Cleaned up `AGENTS.md` — Removed GitHub workflow (now in orchestrator), kept only coding standards and testing requirements
- Updated all caching-strategies unit documents to use Valkey instead of Redis — fsd.md, user_stories.md, bsd.md, problem_space.md now reference BackendValkey as the sole distributed cache backend
- Simplified caching architecture to Valkey-only — removed Otter v2 dependency, removed pluggable backend abstraction, removed BackendType enum. Valkey runs in ALL environments including local dev via Docker Compose.

## Notes
- All scenarios mapped to BSD success criteria with Must/Should/Could priorities
- FSD covers pluggable backend interface, stampede protection, and observability integration
- Orchestrator now has: Core Rules, Memory System, Workflow, QA Process, Agent Management, Usage Patterns, Git & PR Workflow
- PR #197 (user_stories.md), PR #198 (fsd.md + orchestrator refactor), PR #199 (research.md)
- Valkey is a HARD REQUIREMENT — no Redis, no Memcached, no alternatives. Valkey runs in ALL environments. One client (valkey-go), one backend (Valkey), all environments.
