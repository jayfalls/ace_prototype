# 2026-03-14

## Changes

### Added
- [PR #99](https://github.com/jayfalls/ace_prototype/pull/99) - docs: update design README with current project state
- [PR #101](https://github.com/jayfalls/ace_prototype/pull/101) - feat: Add agent workflow integration
  - Added `.openhands/setup.sh` for idempotent environment setup (installs Go, Node.js, Docker, clones agency-agents repo)
  - Added `.openhands/pre-commit.sh` for quality gates (Go build, SQLC validation, tests, frontend lint)
  - Added `agency-agents/` to `.gitignore`
  - Updated `AGENTS.md` with specialist activation mapping for 14 workflow stages

- **messaging-paradigm**: Problem Space document
  - Defined message envelope with mandatory fields (message_id, correlation_id, agent_id, cycle_id, source_service, timestamp, schema_version)
  - NATS subject structure: `ace.<domain>.<agentId>.<subsystem>.<action>`
  - Agent ID enforcement as first-class segment
  - Communication patterns: request-reply, fire-and-forget, streaming
  - Service connection management with shared wrapper
  - Additional problems: error handling, ordering, competing consumers, retention, testing, health checks
  - PR [#102](https://github.com/jayfalls/ace_prototype/pull/102)

- **messaging-paradigm**: BSD document
  - Business case for establishing communication contracts
  - Business value: consistency, observability, debuggability, scalability
  - Risk if not done
  - Scope: in scope and out of scope items
  - Open questions for FSD
  - Priority: critical foundation
  - PR [#103](https://github.com/jayfalls/ace_prototype/pull/103)

- **messaging-paradigm**: User Stories document
  - 14 user stories covering all aspects of messaging paradigm
  - Envelope, subjects, patterns, wrapper, health checks, testing, validation
  - Error handling, JetStream configuration, stream ownership, ordering
  - PR [#104](https://github.com/jayfalls/ace_prototype/pull/104)

- **core-api**: Implemented response helpers in `backend/services/api/internal/response/response.go`
  - `APIResponse` struct for standardized success responses
  - `APIError` struct for error responses with code, message, and field details
  - `FieldError` struct for validation field-level details
  - Helper functions: `Success`, `Created`, `Error`, `ValidationError`
  - HTTP status helpers: `BadRequest`, `Unauthorized`, `Forbidden`, `NotFound`, `InternalError`
  - `JSON` helper for raw JSON responses
  - Added comprehensive tests in `response_test.go`

- **core-api**: Implemented input validation using go-playground/validator
  - Added validation package in `backend/services/api/internal/validator/`
  - Created example handler in `backend/services/api/internal/handler/example.go` demonstrating validation pattern
  - Validation returns 400 with field-level error details
  - Validation happens before business logic
  - Added comprehensive tests in `validator_test.go` and `example_test.go`

- **core-api**: Completed configuration package
  - All config from environment variables (database, API, CORS, logging, JWT)
  - Validation on startup (required fields, JWT secret length, log level)
  - Created `.env.example` documenting all required environment variables
  - Added comprehensive tests for all config options

## Notes
- Resolves issue #86 - Response helpers (core-api)
- Resolves issue #87 - Input validation (core-api)
- Resolves issue #88 - Configuration package (core-api)
