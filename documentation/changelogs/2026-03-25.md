# 2026-03-25

## Changes

### Fixed
- Fixed Makefile test target path: `/app/backend/scripts/docs-gen` -> `/app/scripts/docs-gen` — was failing with "can't cd to" error
- Added `./scripts/docs-gen` to `backend/go.work` workspace — was failing with "module not in workspace" error
- Updated QA agent instructions to mandate `make test` execution for all code changes
- Updated orchestrator instructions clarifying QA agent responsibility for test execution
- Added documentation volume mount to devops/dev/compose.yml for docs-gen
- Added docs-gen module files to api.Containerfile build context
- Fixed docs-gen `findRepoRoot()` to check `/documentation` mount as fallback when `.git` not found
- Fixed validate.go to only count column lines (not constraint lines)
- Changed default API URL from localhost to 127.0.0.1 to avoid DNS issues
- Updated Makefile to dynamically get database IP via docker inspect

### Changed
- QA agent now requires explicit `make test` command execution — no skipping test verification
